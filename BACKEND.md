# معماری رتبه‌شو — React (فرانت) + Node.js (بک‌اند)

سایت به‌صورت دو لایهٔ کاملاً جدا طراحی شده تا برای کارهای سنگین مقیاس‌پذیر باشد و سئو به‌راحتی تقویت شود.

```
┌────────────────────────┐        HTTPS / REST         ┌────────────────────────┐
│   فرانت‌اند (React)      │  ─────────────────────────▶  │   بک‌اند (Node.js)       │
│   Vite + React Router  │   src/api/client.ts          │   Express + SQLite     │
│                        │   (fetch + JWT)              │   JWT + bcrypt + zod   │
└────────────────────────┘                              └────────────────────────┘
        ▲                                                        │
        │  حالت دمو (بدون سرور): localStorage                     ▼
        └──────────────────────────────────────────────  server/data/ratbesho.db
```

## دو حالت اجرا

| حالت | شرط | محل ذخیرهٔ داده |
|---|---|---|
| **دمو** | `.env` نباشد / `VITE_API_BASE_URL` خالی | localStorage مرورگر |
| **واقعی** | `VITE_API_BASE_URL` تنظیم شده | سرور Node.js (SQLite) |

سوییچ بین این دو حالت **فقط در `src/api/client.ts`** انجام می‌شود؛ بقیهٔ کد (کامپوننت‌ها و صفحات) هیچ اطلاعی از محل داده ندارند. یعنی بعداً برای تغییر بک‌اند یا رفتن روی دیتابیس بزرگ، حتی یک کامپوننت هم تغییر نمی‌کند.

## راه‌اندازی کامل (هر دو لایه)

**۱) بک‌اند:**
```bash
cd server
npm install
npm start          # → http://localhost:4000
```

**۲) فرانت‌اند:**
```bash
# در ریشهٔ پروژه
cp .env.example .env
npm run dev        # → http://localhost:5173
```

حالا فرم‌ها (ثبت‌نام، رزرو مشاوره، خرید، پیام) واقعاً به سرور Node.js می‌روند و در SQLite ذخیره می‌شوند.

## چرا این معماری برای سئو خوب است؟

- **جدایی کامل فرانت و بک** → می‌توانی فرانت را بعداً به **Next.js (SSR/SSG)** مهاجرت کنی تا صفحات سمت سرور رندر شوند؛ بدون دست زدن به بک‌اند.
- **لایهٔ API واحد (`src/api`)** → همهٔ داده‌ها از یک نقطه می‌آیند، پس افزودن کش، CDN و pre-render ساده است.
- متاتگ‌ها، Open Graph، JSON-LD (دادهٔ ساختاریافته)، `sitemap.xml` و `robots.txt` از قبل آماده‌اند.
- روت‌ها تمیز (`/classes` نه `/#/classes`) با تنظیمات redirect برای Netlify/Vercel.

## مسیر ارتقا (Roadmap)

1. **الان:** Express + SQLite — مناسب شروع و ترافیک متوسط.
2. **بعدی:** تعویض `server/src/db.js` با **PostgreSQL + Prisma** (امضای توابع ثابت می‌ماند).
3. **سئوی پیشرفته:** مهاجرت فرانت به **Next.js** برای SSR و رندر سمت سرور.
4. **مقیاس:** افزودن Redis برای کش، و CDN برای assets.

## حساب‌های نمونه

| نقش | ایمیل | رمز |
|---|---|---|
| ادمین | `admin@ratbesho.ir` | `admin1405` |
| دانش‌آموز | `negar@test.ir` | `123456` |

جزئیات endpointها در [`server/README.md`](server/README.md) آمده است.
